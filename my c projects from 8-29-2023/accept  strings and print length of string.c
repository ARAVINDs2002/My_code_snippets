#include <stdio.h>

void main()
{
   char a[20],i,length;
  
   printf("enter the characters\n");
  
      gets(a);
      length=strlen(a);
      printf("the length of string is %d\n",length);
      
}
